import { handleImages } from "../src/client/js/handleImages";

describe('handleImages function test', () => {
    test('Check to see if function returns something', () => {
      expect(handleImages).toBeDefined();
    })
  })