import { updateForecast } from "../src/client/js/updateForecast";

describe('updateForecast function test', () => {
    test('Check to see if function returns something', () => {
      expect(updateForecast).toBeDefined();
    })
  })