import { updateImages } from "../src/client/js/updateImages";

describe('updateImages function test', () => {
    test('Check to see if function returns something', () => {
      expect(updateImages).toBeDefined();
    })
  })