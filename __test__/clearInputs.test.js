import { clearInputs } from "../src/client/js/clearInputs";

describe('clearInputs function test', () => {
    test('Check to see if function returns something', () => {
      expect(clearInputs).toBeDefined();
    })
  })