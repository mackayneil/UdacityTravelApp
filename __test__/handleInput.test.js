import { handleInput } from "../src/client/js/handleInput";

describe('handleInput function test', () => {
    test('Check to see if function returns something', () => {
      expect(handleInput).toBeDefined();
    })
  })