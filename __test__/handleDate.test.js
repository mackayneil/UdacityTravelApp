import { handleDate } from "../src/client/js/handleDate";

describe('handleDate function test', () => {
    test('Check to see if function returns something', () => {
      expect(handleDate).toBeDefined();
    })
  })