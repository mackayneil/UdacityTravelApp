import { updateCurrentDate } from "../src/client/js/updateCurrentDate";

describe('updateCurrentDate function test', () => {
    test('Check to see if function returns something', () => {
      expect(updateCurrentDate).toBeDefined();
    })
  })